package ${package.Service};

import ${package.Entity}.${entity};
import ${superServiceClassPackage};
import ${cfg.queryParamPath};
import ${cfg.queryVoPath};
import io.geekidea.fastspringboot.common.web.vo.Paging;

/**
 * <p>
 * $!{table.comment} 服务类
 * </p>
 *
 * @author ${author}
 * @since ${date}
 */
#if(${kotlin})
interface ${table.serviceName} : ${superServiceClass}<${entity}>
#else
public interface ${table.serviceName} extends ${superServiceClass}<${entity}> {
    /**
     * 根据ID获取查询对象
     * @param id
     * @return
     */
    ${entity}QueryVo getById(String id) throws Exception;

    /**
     * 获取分页对象
     * @param ${entity.substring(0, 1).toLowerCase()}${entity.substring(1)}QueryParam
     * @return
     */
    Paging<${entity}QueryVo> getPageList(${entity}QueryParam ${entity.substring(0, 1).toLowerCase()}${entity.substring(1)}QueryParam) throws Exception;

}
#end
